<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ResepMakanan"
        tools:targetApi="31">

        <!-- MainActivity -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other activities -->
        <activity android:name=".activities.DetailActivity" android:exported="false" />
        <activity android:name=".activities.Forgotpass" android:exported="false" />
        <activity android:name=".activities.ConfirmPass" android:exported="false" />
        <activity android:name=".activities.Homepage_main" android:exported="false" />
        <activity android:name=".activities.LoginPages" android:exported="true" />
        <activity android:name=".activities.SignUp" android:exported="true" />

    </application>
</manifest>